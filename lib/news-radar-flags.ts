// 소식 레이더 피처 플래그.
//   PET_CONTENT_ENABLED=false(기본): 반려동물(펫) 분야는 주간 자동 수집·점수화에서 제외하고
//   공개 밴드(NewsRadarBand)에도 노출하지 않는다. 관리자 검색창의 수동 펫 검색은 허용(온디맨드).
//   펫 상품 라인 출시 시 true 로 바꾸면 자동 수집·공개에 펫이 포함된다.
export const PET_CONTENT_ENABLED = false;
